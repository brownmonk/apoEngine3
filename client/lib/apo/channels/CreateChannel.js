// Client EyeContact Channel
define([], function(){
  return function(options){
    if(options){
      var channelName = options.channelName;
      var outbound    = options.outbound;
      var inbound     = options.inbound;

      var newChannel = {};
      return newChannel;
    }
  }
});